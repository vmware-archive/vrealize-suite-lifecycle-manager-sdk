Read me -

Environment variables : -

vRSLCM_URL - https://vRSLCMhostName
vIDM_URL - https://vIDMhostName 

vIDM as Auth source : -

1. Make sure vIDM AD use has enough permissions to authenticate LCM apis
2. In order to use vIDM auth token as Auth source for LCM apis , user needs to select Auth type as Bearer token
